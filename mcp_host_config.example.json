{
  "_comment": "Add this 'mcpServers' block to your MCP host's config file. Use ABSOLUTE paths (the host launches the server from its own working directory, so relative paths break). On Windows the command is .venv\\Scripts\\docqa-server.exe.",
  "mcpServers": {
    "docqa": {
      "command": "/ABSOLUTE/PATH/TO/mcp-docqa-server/.venv/bin/docqa-server",
      "env": {
        "DOCQA_SQLITE_PATH": "/ABSOLUTE/PATH/TO/mcp-docqa-server/data/index.db"
      }
    }
  }
}
